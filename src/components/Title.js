// Title.js: 메인/섹션 타이틀을 표시하는 간단한 컴포넌트입니다.
// 주요 기능:
// - 중앙 정렬된 제목을 보여줍니다.
// - 스타일은 csst1 객체로 지정합니다.

import React from "react";

const Title = () => {
  let csst1 = {
    marginTop: "70px",
    textAlign: "center",
  };
};

export default Title;
